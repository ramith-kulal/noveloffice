import { Container, Typography } from '@mui/material';

const About: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">About</Typography>
      <Typography>About the Loan Calculator App</Typography>
    </Container>
  );
};

export default About;